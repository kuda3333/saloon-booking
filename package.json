{
  "name": "saloon-booking",
  "version": "1.1.0",
  "private": true,
  "description": "Glam Studio — online salon booking app (Vercel + Firebase + Resend + Twilio)",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "firebase-admin": "^12.6.0"
  },
  "engines": {
    "node": ">=18"
  }
}
